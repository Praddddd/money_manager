{
  "buildCommand": "curl https://storage.googleapis.com/flutter_infra_release/releases/stable/windows/flutter_windows_3.24.0-stable.zip -o flutter.zip && unzip -q flutter.zip && export PATH=$PATH:$PWD/flutter/bin && flutter pub get && flutter build web --release",
  "outputDirectory": "build/web"
}
